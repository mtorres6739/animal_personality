# Database Configuration
# Option 1: Direct connection string (recommended for production)
CONNECTION_STRING_DIRECT=postgresql://username:password@host:port/database

# Option 2: Standard DATABASE_URL
DATABASE_URL=postgresql://username:password@host:port/database

# Option 3: Postgres URL (Vercel/Neon format)
POSTGRES_URL=postgresql://username:password@host:port/database?sslmode=require

# Email Configuration (Brevo/SendinBlue)
BREVO_API_KEY=your-brevo-api-key-here
FROM_EMAIL=noreply@yourdomain.com

# For local development without a database, you can leave these empty
# The app will still function but won't save results